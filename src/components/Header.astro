---

---

<header
  class="relative flex flex-col items-center justify-center pt-4 md:pt-8 px-4 mb-10 max-w-4xl mx-auto"
>
  <h1 class="text-5xl font-bold mb-6">Split It!</h1>
  <div class="barra absolute">
    <div class="bg-red-600 dark:bg-red-500 rounded"></div>
  </div>
  <h2 class="text-xl font-medium uppercase text-center text-pretty max-w-80 lg:max-w-none">
    <span class="text-green-600 dark:text-green-400">Organiza</span> y <span class="text-orange-500 dark:text-orange-300"
      >comparte</span
    > tus gastos con quien tu quieras
  </h2>
</header>

<style>
  .barra {
    width: 250px;
    height: 8px;
    top: 0;
    left: 50%;
    transform: translate(-50%, 40px) rotateZ(-4deg);
    padding: 2px 0;

    @media (width >= 768px) {
      transform: translate(-50%, 56px) rotateZ(-4deg);
    }

    @media (prefers-color-scheme: light) {
      background-color: rgb(240, 240, 240);
    }
    @media (prefers-color-scheme: dark) {
      background-color: rgb(32, 32, 32);
    }

    & > div {
      width: 100%;
      height: 3px;
    }
  }
</style>
